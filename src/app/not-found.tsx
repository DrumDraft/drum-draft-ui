import { NotFoundMessage } from "@/widgets/not-found/ui/NotFoundMessage";

export default function NotFound() {
  return (
    <div className="min-h-screen w-full flex flex-col justify-center">
      <NotFoundMessage></NotFoundMessage>
    </div>
  );
}
